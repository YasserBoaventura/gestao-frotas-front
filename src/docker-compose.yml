version: '3.8'

services:
  frontend:
    build: .
    container_name: fleet-manager-frontend
    ports:
      - "4200:80"
    networks:
      - fleet-network

networks:
  fleet-network:
    driver: bridge
    external: true
